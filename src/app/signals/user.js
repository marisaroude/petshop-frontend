import { signal } from '@preact/signals-react'

export const userGoogleInfo = signal(null)
export const userFormRegister = signal(null)
export const currentUser = signal(null)
export const allUsers = signal(null)
